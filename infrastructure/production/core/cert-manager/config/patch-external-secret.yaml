apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-api-token-secret
  namespace: cert-manager
spec:
  data:
    - secretKey: api-token
      remoteRef:
        key: kubernetes/production/cert-manager/cloudflare-api-token
        property: api-token
