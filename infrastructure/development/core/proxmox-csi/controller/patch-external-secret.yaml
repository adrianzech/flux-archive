apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: proxmox-csi-plugin
  namespace: csi-proxmox
spec:
  data:
    - secretKey: token_secret
      remoteRef:
        key: kubernetes/development/csi-proxmox/proxmox-csi-plugin
        property: token_secret
